# @hudhod/core

## Unreleased

- Base primitives: `Disposable`, `DisposableStore`, `Emitter`, `CancellationTokenSource`.
- Path utilities and typed `HudhodError` factories.
- Provider-backed file system with in-memory and WebContainer adapters, watching,
  workspace configuration, search, diff, and managed processes.
- `CommandRegistry`, `ServiceRegistry`, manifest validation, and the first-party
  `InProcessExtensionHost`.
